# --- Production Database Credentials ---
# Used for creating a backup (read-only access is sufficient)
PROD_DB_HOST=your_prod_host.com
PROD_DB_USER=your_prod_user
PROD_DB_PASSWORD=your_prod_password
PROD_DB_NAME=your_prod_dbname
PROD_DB_PORT=5432

# --- Local Database Credentials ---
# Used for restoring the backup and running tests (superuser access may be needed to drop/create DB)
LOCAL_DB_HOST=localhost
LOCAL_DB_USER=your_local_user
LOCAL_DB_PASSWORD=your_local_password
LOCAL_DB_NAME=curiosity_coach_test
LOCAL_DB_PORT=5432

# --- Service URLs ---
# The URLs where your local backend and brain services are running
BACKEND_API_URL=http://127.0.0.1:8000

# --- Test User Data ---
# A valid phone number for a user that exists in the production database
TEST_USER_PHONE_NUMBER="1555123456"